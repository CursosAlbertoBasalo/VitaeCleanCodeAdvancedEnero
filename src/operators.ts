import { Booking } from "./booking";

export class Operators {
  public verifyAvailability(booking: Booking): boolean {
    console.log(booking);
    return undefined;
  }
  public reserveBooking(booking: Booking): string {
    console.log(booking);
    return undefined;
  }
  public releaseBooking(booking: Booking): void {
    console.log(booking);
    return undefined;
  }
}
